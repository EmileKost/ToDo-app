// const express = require("express");
// const router = express.Router();

// //You change app in router here because we use router
// // Whe don't use /register bc the app already uses register automatically
// router.get("/", (req, res) => {
//   res.render("login");
// });

// module.exports = router;
